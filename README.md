# my-travel-app
AI travel-photo app monorepo: Flutter mobile, FastAPI backend, Python ML, FFmpeg renderer, Docker Compose for local.

## Quickstart
```bash
docker compose -f infra/docker-compose.yml up --build

Mobile (first time):

cd app-mobile
flutter pub get
flutter run
```